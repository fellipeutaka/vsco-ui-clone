import { BiCookie } from "react-icons/bi";
import { Container } from "./styles";

export default function CookieButton() {
  return (
    <Container title="Cookies Settings">
      <BiCookie size="32px" />
    </Container>
  );
}
